export {
    PLACEHOLDER_REGEX,
    normalizePlaceholderKey,
    extractTemplatePlaceholders,
    buildDocumentMappingContext,
    buildCertificateMappingContext,
    resolveField,
    sharedFieldResolver,
    resolveDocumentPlaceholderValue,
    resolveCertificatePlaceholderValue,
    buildResolvedMappings,
    buildResolvedTemplateConfig,
    applyMappingsToTemplateElements,
    normalizeTemplateLayout,
} from '@/lib/shared-field-resolver';
