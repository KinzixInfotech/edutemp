/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["utfs.io", "uploadthing.com"], // ← REQUIRED for image display
    },
  };
  
  export default nextConfig;